# .readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"
  jobs:
    build:
      steps:
        - commands:
            - pip install -r requirements.txt
python:
  install:
    - method: pip
      path: .
sphinx:
  configuration: docs/conf.py
